import React from 'react';

const Predictor = () => {

    // Functions
    React.useEffect(() => {
        return (() => { });
    });

    return <div>Interactive Map</div>;
};

export default Predictor;